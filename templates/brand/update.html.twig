{% extends 'base.html.twig' %}

{% block title %}Update Brand{% endblock %}

{% block body %}
<div class="container">
    <h2 class="mt-5 mb-5 text-center text-primary">UPDATE BRAND</h2>
    {{ form_start (form) }}
        <div class="form-group">
            <h6 class="text-primary">Name</h6>
            {{ form_widget (form.Name, {'attr' : {'class' : 'form-control'}}) }}
        </div>
        <div class="form-group mt-2">
            <h6 class="text-primary">Description</h6>
            {{ form_widget (form.Description, {'attr' : {'class' : 'form-control'}}) }}
        </div>
        <div class="form-group mt-2">
            <h6 class="text-primary">Year</h6>
            {{ form_widget (form.Year, {'attr' : {'class' : 'form-control'}}) }}
        </div>
        <div class="form-group mt-2">
            <h6 class="text-primary">Logo</h6>
            {{ form_widget (form.ImageUpload, {'attr' : {'class' : 'form-control'}}) }}
        </div>
        <div class="form-group text-center mt-5">
            <button type="submit" class="btn btn-success mt-2">Update</button>
        </div>
    {{ form_end (form) }}
    <div class="col-md-12 text-center">
        <a href="{{ asset('manager/brand') }}" class="btn btn-secondary" role="button">Back</a>
    </div>
</div>
{% endblock %}
